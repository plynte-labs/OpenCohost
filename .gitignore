# ── Directorios del proyecto ──
logs/
Grabaciones/
__pycache__/
legacy/
# Exception: the OpenCohost RF4 preservation module is intentionally tracked
# (Phase 6 Stage 2a — revivable parked surface). Re-include the dir + contents.
!opencohost/ui/legacy/
!opencohost/ui/legacy/**
modelos_f5/
temp/
test/

# ── Archivos específicos ──
result.txt
result_predict.txt

# ── Configuración generada en runtime ──
# Dev-mode USER_DATA_DIR is the repo root (see opencohost/config/storage.py)
config/ptt_settings.json
config/window_geometry.json
config/music_library.json
config/last_model.json
config/llm_tiers.json
config/tts_local_only.json
config/tts_speed.json
config/avatar.yaml
# Minted API bearer tokens (live secret — never commit)
config/api_tokens.json
opencohost/config/api_tokens.json
# Stale pre-restructure leftovers; runtime never writes here anymore
opencohost/config/ptt_settings.json
opencohost/config/window_geometry.json
logs/acciones.jsonl
data/smart_aggregator/
data/stream_admin/oauth_client.json
data/stream_admin/oauth_tokens.json
data/stream_admin/tokens/
data/
.coverage

# ── Python ──
*.py[cod]
*.pyo
*.egg-info/
dist/
build/

# ── Launcher build artifacts ──
# _release_meta.py is generated at release-build time (see packaging/build_release_meta.py)
packaging/_release_meta.py
packaging/vendor/
*.egg
*.whl

# ── Entorno virtual ──
venv/
.venv/
env/
.env
*.env

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── OS ──
Thumbs.db
.DS_Store
Desktop.ini

# ── SDD / Conductor artifacts ──
.atl/
openspec/
sdd/
conductor/

# ── Session notes and drafts ──
session-*.md
.pr_body.md

# ── Debug scripts ──
debug_*.py
debug_obs.py
mudanza.py

# ── Local tooling dirs ──
.opencode/
.agents/
.orchestrator/
.codegraph/
.claude/
VoiceAItemp*/

# ── User assets ──
assets/music/
opencohost/config/music_library.json
Documents/
WhatIs.md
opencohost/config/last_model.json

# ── Runtime user state (seeded from opencohost/config/default_profiles.json on first run) ──
perfiles.json
cohost_profiles.json
CLAUDE.local.md
opencode.json

# Engram persistent memory database (internal)
.engram/

# ── Local documentation ──
docs/reportar_falso_positivo.md


# Local UI prototype
OpenCohost_TAURI/

# React/Tauri frontend — versioned in its own git repo (migration; separate from the Python repo)
OpenCohost_UI/

# Runtime user config (contains api_tokens.json — secrets must never land in git).
# Anchored to the repo root: an unanchored `config/` also swallowed the tracked
# opencohost/config/ package (new source files there became invisible to git).
/config/
.pnpm-store/

# ── Internal development records (owner ruling 2026-08-02) ──
AGENT_HANDOFF.md
docs/sesiondanger/
docs/audit_snapshots/
docs/HANDOFF_RF*.md
docs/AGENT_*_INSTRUCTIONS.md
docs/*technical_session*.md

# ── Hygiene ──
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pyright/
.hypothesis/
/models/
/checkpoints/
/weights/
*.safetensors
*.ckpt
*.gguf
*.ggml
*.onnx
*.pt
*.pth
*.key
*.pem
*.p12
*.pfx
.cache/
*.bak
# Dated backups (perfiles.json.bak-20260809) escape the bare *.bak above and are
# live user data; a review candidate froze one into scope on 2026-08-11.
*.bak-*
*.orig
