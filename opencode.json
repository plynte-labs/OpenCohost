{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "safe-stage-check": {
      "description": "Review Engram + Git state and recommend safe staging commands without modifying files.",
      "template": "Run `python tools/safe_stage_check.py` from the repository root. Do not stage, commit, clean, reset, or modify files. Summarize the Safe Stage Check verdict and exact recommended commands, if any."
    }
  }
}
